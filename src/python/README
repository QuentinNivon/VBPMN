##### How to compare two CIF files? #####

$ python pif2lnt file1.cif file2.cif operation

-> This command converts each file into an LTS and compares them
   wrt. the operation

-> Operations supported so far:

   ">" the first LTS simulates the second LTS

   "<" the second LTS simulates the first LTS

   "=" both LTSs are trace equivalent
